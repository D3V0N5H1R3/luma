// The LumaConfig accessor and the luma_config singleton are generated from
// extensions/shared/defaults.json so that the set of getters always tracks the
// declared settings. Edit defaults.json and run generate-config-code.py rather
// than hand-editing the accessor. See generated/config-accessor.ts.
export { LumaConfig, luma_config } from "../generated/config-accessor";

# ─────────────────────────────────────────────────────────────
# Luma — CodeQL Analysis Workflow (advanced setup)
#
# Runs CodeQL static analysis on every push and PR targeting main,
# plus a weekly scheduled scan. Each first-party language is
# analysed with CodeQL's buildless extractor (build-mode: none):
# no project build is required, which keeps the scan fast and
# robust. For slightly higher C/C++ precision you can switch the
# c-cpp entry to build-mode: manual (see the commented block).
# ─────────────────────────────────────────────────────────────

name: "CodeQL Advanced"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 4 * * 1" # Weekly Monday 04:23 UTC.

permissions:
  actions: read
  contents: read
  packages: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 90

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: c-cpp
            build-mode: manual
          - language: actions
            build-mode: none
          - language: javascript-typescript
            build-mode: none
          - language: python
            build-mode: none

    steps:
      - name: Checkout source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Initialize CodeQL
        uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          config-file: ./.github/codeql/codeql-config.yml

      - name: Build C++ (CodeQL manual mode)
        if: matrix.language == 'c-cpp'
        run: |
          cmake --preset default
          cmake --build --preset default -- -j2

      - name: Ensure CodeQL trap cache directory exists
        if: matrix.language == 'c-cpp'
        run: mkdir -p "$RUNNER_TEMP/trapCaches/cpp/tarballs"

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          category: "/language:${{ matrix.language }}"
